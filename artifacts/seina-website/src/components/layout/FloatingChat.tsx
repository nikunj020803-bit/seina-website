export function FloatingChat() {
  return (
    <a
      href="#"
      className="fab"
      aria-label="Chat with seina"
      onClick={(e) => {
        e.preventDefault();
        // Chat widget placeholder
      }}
      data-testid="fab-chat"
    >
      s
    </a>
  );
}
